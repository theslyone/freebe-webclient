angular
  .module('theme.core.bank_account_controller', [
    'ngGrid'
  ])
  .controller('BankAccountController', ['$scope', '$filter', '$ocLazyLoad', function($scope, $filter, $ocLazyLoad) {
    'use strict';
  }]);
